_start:
	lookup unit utype lookup
	ubind utype
	jump _nextunittype equal firstuid @unit
	jump _flagreset notEqual loop 0
	set firstuid @unit
_flagreset:
	sensor controlstat @unit @controlled
	sensor flag @unit @flag
	jump _nextloop equal flag 0
	jump _nextloop notEqual controlstat 0
	ucontrol flag 0 0 0 0 0
_nextloop:
	op add loop loop 1
	end
_nextunittype:
	op add lookup lookup 1
	jump _lookup35reset lessThan lookup 35
	set lookup 0
_lookup35reset:
	set loop 0
	set firstuid 0
	print "\code by acopika"
_end:

